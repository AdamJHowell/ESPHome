# tmp117.yaml
# TMP117 Sensor Configuration
# https://esphome.io/components/sensor/tmp117.html
platform:            tmp117
name:                "TMP117 Temperature"
address:             0x48
update_interval:     20s
id:                  tmp117_temp
device_class:        "temperature"
unit_of_measurement: "°C"
accuracy_decimals:   1

# MQTT is removed because I currently have this only on a RP2040 which doesn't support MQTT in ESPHome.
#on_value:
#  - mqtt.publish:
#      topic:   "$mqtt_location/$mqtt_device/tmp117/temp"
#      payload: return to_string( id( tmp117_temp ).state );
